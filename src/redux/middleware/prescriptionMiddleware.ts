// import { handlePrescriptionBasedLensData } from '@/redux/ReducerFunctions/userLensReducer';
// import { Middleware } from '@reduxjs/toolkit';

// // eslint-disable-next-line @typescript-eslint/no-unused-vars
// export const prescriptionMiddleware: Middleware = store => next => action => {
//   const result = next(action);

//   if (handlePrescriptionBasedLensData.match(action)) {
//     action.payload.prescriptionBasedLensFormData;
//     const data = action.payload.prescriptionBasedLensFormData;
//     localStorage.setItem('prescriptionBasedLensFormData', JSON.stringify(data));
//   }

//   return result;
// };
